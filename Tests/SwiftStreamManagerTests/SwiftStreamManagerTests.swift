import XCTest
@testable import SwiftStreamManager

final class SwiftStreamManagerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        //XCTAssertEqual(SwiftStreamManager().text, "Hello, World!")
    }
}
